# Set this to your deployed backend URL when frontend is hosted separately.
# Example (Render): https://your-api-service.onrender.com
# Leave empty when frontend is built into the Spring Boot static folder (same origin).
VITE_API_URL=
